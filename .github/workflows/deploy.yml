name: Zcash Radio — Build & Deploy

on:
  push:
    branches: [ main ]
  schedule:
    - cron: "0 */6 * * *"   # refresh every 6 hours
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      # Build + run the Rust scanner to (re)generate public/videos.json
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Generate videos.json
        run: |
          cargo run --release --manifest-path zcash-radio-scan/Cargo.toml -- \
            --topic-url https://forum.zcashcommunity.com/t/what-are-you-listening-to/20456 \
            --out public/videos.json

      # Prepare and upload the static site
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    needs: build
    runs-on: ubuntu-24.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

